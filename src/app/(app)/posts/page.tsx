import { SectionStub } from "@/components/section-stub";

export const metadata = { title: "Posts" };

export default function Page() {
  return (
    <SectionStub
      title="Posts"
      description="Build a post: pick up to 10 photos, write the caption, attach hashtags, and tag other accounts."
      phase="phase 3"
    />
  );
}
