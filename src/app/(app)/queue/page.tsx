import { SectionStub } from "@/components/section-stub";

export const metadata = { title: "Queue" };

export default function Page() {
  return (
    <SectionStub
      title="Queue"
      description="Your weekly time slots, the rolling queue, and any fixed-date posts."
      phase="phase 4"
    />
  );
}
