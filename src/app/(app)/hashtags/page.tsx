import { SectionStub } from "@/components/section-stub";

export const metadata = { title: "Hashtag library" };

export default function Page() {
  return (
    <SectionStub
      title="Hashtag library"
      description="Your saved hashtags, grouped into categories like location or shoot type, ready to filter and attach when building a post."
      phase="phase 3"
    />
  );
}
