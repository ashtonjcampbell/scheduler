import { SectionStub } from "@/components/section-stub";

export const metadata = { title: "Grid preview" };

export default function Page() {
  return (
    <SectionStub
      title="Grid preview"
      description="A three-column mock of your profile grid, showing published and scheduled posts in order."
      phase="phase 5"
    />
  );
}
