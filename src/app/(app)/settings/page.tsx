import { SectionStub } from "@/components/section-stub";

export const metadata = { title: "Settings" };

export default function Page() {
  return (
    <SectionStub
      title="Settings"
      description="Connect Instagram, switch off dry run, and set your posting slots."
      phase="phase 6"
    />
  );
}
