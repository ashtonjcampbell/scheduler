import { SectionStub } from "@/components/section-stub";

export const metadata = { title: "Ideas" };

export default function Page() {
  return (
    <SectionStub
      title="Ideas"
      description="Loose ideas, optionally paired with photos, plus a freeform rich-text notepad."
      phase="phase 5"
    />
  );
}
